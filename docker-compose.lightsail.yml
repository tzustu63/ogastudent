version: '3.8'

services:
  # PostgreSQL 資料庫（完全獨立）
  postgres:
    image: postgres:15-alpine
    container_name: isvs-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_NAME:-foreign_student_verification}
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:?請設定 DB_PASSWORD 環境變數}
      POSTGRES_INITDB_ARGS: "--encoding=UTF8 --locale=C"
    ports:
      - "${DB_PORT:-5434}:5432"  # 使用 5434 避免衝突
    volumes:
      - isvs_postgres_data:/var/lib/postgresql/data
      - ./database/init:/docker-entrypoint-initdb.d:ro
    networks:
      - isvs-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres} -d ${DB_NAME:-foreign_student_verification}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # Redis 快取（完全獨立）
  redis:
    image: redis:7-alpine
    container_name: isvs-redis
    restart: unless-stopped
    command: >
      sh -c "
      if [ -n \"$${REDIS_PASSWORD}\" ]; then
        redis-server --appendonly yes --requirepass \"$${REDIS_PASSWORD}\"
      else
        redis-server --appendonly yes
      fi
      "
    ports:
      - "${REDIS_PORT:-6380}:6379"  # 使用 6380 避免衝突
    volumes:
      - isvs_redis_data:/data
    networks:
      - isvs-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # MinIO 檔案儲存 (S3 相容)（完全獨立）
  minio:
    image: minio/minio:latest
    container_name: isvs-minio
    restart: unless-stopped
    command: server /data --console-address ":9003"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:?請設定 MINIO_ROOT_USER 環境變數}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:?請設定 MINIO_ROOT_PASSWORD 環境變數}
    ports:
      - "${MINIO_PORT:-9002}:9000"  # 使用 9002 避免衝突
      - "${MINIO_CONSOLE_PORT:-9003}:9003"  # 使用 9003 避免衝突
    volumes:
      - isvs_minio_data:/data
    networks:
      - isvs-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # 後端 API (生產環境)
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: isvs-backend
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 5000
      LOG_LEVEL: ${LOG_LEVEL:-info}
      
      # 資料庫配置
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: ${DB_NAME:-foreign_student_verification}
      DB_USER: ${DB_USER:-postgres}
      DB_PASSWORD: ${DB_PASSWORD:?請設定 DB_PASSWORD 環境變數}
      
      # Redis 配置
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD:-}
      
      # JWT 配置
      JWT_SECRET: ${JWT_SECRET:?請設定 JWT_SECRET 環境變數}
      JWT_EXPIRES_IN: ${JWT_EXPIRES_IN:-24h}
      JWT_REFRESH_EXPIRES_IN: ${JWT_REFRESH_EXPIRES_IN:-7d}
      
      # 檔案上傳配置
      UPLOAD_MAX_SIZE: ${UPLOAD_MAX_SIZE:-10485760}
      UPLOAD_ALLOWED_TYPES: ${UPLOAD_ALLOWED_TYPES:-pdf,doc,docx,jpg,jpeg,png}
      
      # MinIO/S3 配置
      AWS_ENDPOINT: http://minio:9000
      AWS_ACCESS_KEY_ID: ${MINIO_ROOT_USER:?請設定 MINIO_ROOT_USER 環境變數}
      AWS_SECRET_ACCESS_KEY: ${MINIO_ROOT_PASSWORD:?請設定 MINIO_ROOT_PASSWORD 環境變數}
      AWS_S3_BUCKET: ${AWS_S3_BUCKET:-foreign-student-docs}
      AWS_REGION: ${AWS_REGION:-us-east-1}
      
      # CORS 配置
      CORS_ORIGIN: ${CORS_ORIGIN:-http://18.181.71.46:3002}
      
      # LDAP 配置（可選）
      LDAP_URL: ${LDAP_URL:-}
      LDAP_BIND_DN: ${LDAP_BIND_DN:-}
      LDAP_BIND_PASSWORD: ${LDAP_BIND_PASSWORD:-}
      LDAP_SEARCH_BASE: ${LDAP_SEARCH_BASE:-}
      
      # 郵件配置
      SMTP_HOST: ${SMTP_HOST:-}
      SMTP_PORT: ${SMTP_PORT:-587}
      SMTP_USER: ${SMTP_USER:-}
      SMTP_PASSWORD: ${SMTP_PASSWORD:-}
      SMTP_FROM: ${SMTP_FROM:-noreply@university.edu.tw}
    ports:
      - "${BACKEND_PORT:-5003}:5000"  # 使用 5003 避免衝突
    volumes:
      - isvs_backend_logs:/app/logs
      - isvs_backend_uploads:/app/uploads
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_healthy
    networks:
      - isvs-network
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:5000/api/health', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # 前端應用 (生產環境)
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL:-http://18.181.71.46:5003/api}
    container_name: isvs-frontend
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-3002}:8080"  # 使用 3002 避免衝突
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - isvs-network
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  isvs_postgres_data:
    driver: local
  isvs_redis_data:
    driver: local
  isvs_minio_data:
    driver: local
  isvs_backend_logs:
    driver: local
  isvs_backend_uploads:
    driver: local

networks:
  isvs-network:
    driver: bridge
    name: isvs-network