# 外國學生受教權查核系統 - 生產環境配置範例
# 複製此檔案為 .env.production 並填入實際值

# ============================================
# 資料庫配置
# ============================================
DB_NAME=foreign_student_verification
DB_USER=postgres
DB_PASSWORD=請設定強密碼
DB_PORT=5432

# ============================================
# Redis 配置
# ============================================
REDIS_PORT=6379
REDIS_PASSWORD=請設定 Redis 密碼（可選）

# ============================================
# MinIO/S3 配置
# ============================================
MINIO_ROOT_USER=請設定 MinIO 使用者名稱
MINIO_ROOT_PASSWORD=請設定強密碼（至少8個字元）
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001
AWS_S3_BUCKET=foreign-student-docs
AWS_REGION=us-east-1

# ============================================
# JWT 配置
# ============================================
JWT_SECRET=請設定強隨機字串（建議使用 openssl rand -base64 32 產生）
JWT_EXPIRES_IN=24h
JWT_REFRESH_EXPIRES_IN=7d

# ============================================
# 應用程式配置
# ============================================
LOG_LEVEL=info
BACKEND_PORT=5001
FRONTEND_PORT=3000
VITE_API_URL=http://localhost:5001/api

# ============================================
# 檔案上傳配置
# ============================================
UPLOAD_MAX_SIZE=10485760
UPLOAD_ALLOWED_TYPES=pdf,doc,docx,jpg,jpeg,png

# ============================================
# LDAP 配置（可選）
# ============================================
LDAP_URL=ldap://your-ldap-server:389
LDAP_BIND_DN=cn=admin,dc=example,dc=com
LDAP_BIND_PASSWORD=ldap-admin-password
LDAP_SEARCH_BASE=ou=users,dc=example,dc=com

# ============================================
# 郵件配置
# ============================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=noreply@university.edu.tw
