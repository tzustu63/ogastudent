# DigitalOcean App Platform 環境變數範例
# 這些變數會在 .do/app.yaml 中自動設定

# ===== 後端環境變數 =====

# JWT 設定（必須）
JWT_SECRET=your-super-secret-jwt-key-for-testing-please-change-in-production
JWT_EXPIRES_IN=24h

# 應用程式設定
NODE_ENV=production
PORT=5000

# 資料庫設定（由 DigitalOcean 自動提供）
# DB_HOST=${postgres-db.HOSTNAME}
# DB_PORT=${postgres-db.PORT}
# DB_NAME=${postgres-db.DATABASE}
# DB_USER=${postgres-db.USER}
# DB_PASSWORD=${postgres-db.PASSWORD}

# Redis 設定（由 DigitalOcean 自動提供）
# REDIS_HOST=${redis-cache.HOSTNAME}
# REDIS_PORT=${redis-cache.PORT}
# REDIS_PASSWORD=${redis-cache.PASSWORD}

# CORS 設定（由 DigitalOcean 自動提供）
# CORS_ORIGIN=${frontend.URL}

# 檔案上傳設定
MAX_FILE_SIZE=10485760
UPLOAD_DIR=/tmp/uploads

# 日誌設定
LOG_LEVEL=info

# Email 設定（可選）
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# ===== 前端環境變數 =====

# 後端 API 網址（由 DigitalOcean 自動提供）
# VITE_API_URL=${backend.URL}/api

NODE_ENV=production
PORT=8080

# ===== DigitalOcean 自動提供的環境變數 =====
# 以下變數會由 DigitalOcean App Platform 自動設定：
# - 資料庫連線資訊
# - Redis 連線資訊
# - 服務間通訊 URL
# - 公開網域
